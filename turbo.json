{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["lint"],
      "outputs": ["coverage/**"]
    },
    "build": {
      "dependsOn": ["test"],
      "outputs": ["dist/**", "bin/**"]
    }
  }
}
